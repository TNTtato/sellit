<%= link_to product_path(product.id), class: "product", "data-turbo-frame": "_top" do%>
  <%= image_tag product.photo.attached? ? product.photo : "default.png", width: 200%>
  <h2><%= product.title %></h2>
  <span><%= product.price%>$</span>
<% end %>